// comment on start
asd = 10;
oko = "t"*5;
/*
  * multiline comment
  */
test = "dupa";
QWE = "other string with 123 + 321";
zxc = 'string with spaces'; //asddaw
rEsult = 0x123 / ( 2 + 123 ) + 23 * asd * ( 2 - 4 );


if(1) {
  print(
    oko + 5 + " super " + // some comment
    test + " RESULT: " + rEsult
  );
} else { 
  "false" // false value
};



if (1) {
  "asd" + 123
};

"test"
